apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "motower-stack.fullname" . }}-test-connection"
  labels:
    {{- include "motower-stack.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "motower-stack.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
