apiVersion: v1
kind: Service
metadata:
  name: {{ include "motower-stack.fullname" . }}
  labels:
    {{- include "motower-stack.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "motower-stack.selectorLabels" . | nindent 4 }}
